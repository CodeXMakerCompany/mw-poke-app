### Local Setup

Run the following commands

cd ...

```
docker-compose up --build
```

Tech Stack:
NextJS + typescript
NodeJS + typescript

Animations : Notion

Patterns
Client
Server DDD

Requirements:

User History:
The user needs to preview the pokemn data in the client, these data comes paginated so the server needs to return the full items in the pokeapi service

Bonus:
○​ Lint and tests (Husky managed)
○​ Using Docker as a local development setup (Using doker and docker compose in the monorepo)
○​ Using Next.js [X]
○​ Using AWS, Terraform and Vercel for deployment and hosting
